@model FlockStudy.Models.PrayerRequest

<div class="glass-card mx-auto max-w-2xl rounded-2xl p-8 shadow-lg">
    <h1 class="text-2xl font-bold text-gray-800">@Model.Title</h1>
    <p class="mt-4 leading-relaxed text-gray-700">@Model.Description</p>
    <p class="mt-2 text-sm text-gray-500">Requested on @Model.CreatedAt.ToShortDateString()</p>

    <a href="/Home/Index" class="mt-6 inline-block rounded-xl bg-orange-600 px-4 py-2 text-white hover:bg-orange-700">
        Back
    </a>
</div>